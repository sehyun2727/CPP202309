#include <iostream>
#include <string>
using namespace std;

int main() {
	int one = 1;
	int two = 2;
	int sum = one + two; //sum값에 one 값과 two값을 더하여 저장
	
	cout << "1+2=" << sum << endl; //sum을 해당 문자열과 함께 출력

	return 0;
}